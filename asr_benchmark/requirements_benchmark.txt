# ASR + Diarization Benchmark — зависимости
# Основные (whisperx, torch, transformers) уже есть в ../requirements.txt

# ── ASR Метрики ────────────────────────────────────────────────
jiwer>=3.0

# ── ASR-движки ─────────────────────────────────────────────────
faster-whisper>=1.0
vosk

# ── Диаризация ─────────────────────────────────────────────────
# pyannote (нужен HF токен + принять лицензию на hf.co/pyannote/speaker-diarization-3.1)
pyannote.audio>=3.1
pyannote.metrics>=3.2

# NeMo (тяжёлая зависимость, ~2GB; раскомментируй если нужно)
# nemo_toolkit[asr]

# SpeechBrain
speechbrain>=1.0

# Кластеризация для SpeechBrain-диаризации
scikit-learn>=1.3

# ── Аудио ──────────────────────────────────────────────────────
pydub       # конвертация mp3 → wav (требует ffmpeg: brew install ffmpeg)
librosa     # загрузка аудио для wav2vec2 и speechbrain
soundfile   # backend для librosa

# ── Утилиты ────────────────────────────────────────────────────
tqdm
pandas
tabulate    # опционально, для красивых таблиц
